mutation JobsiteAddDefaultTruckingRateToAll(
  $itemIndex: Int!
  $rateIndex: Int!
) {
  jobsiteAddDefaultTruckingRateToAll(
    systemRateItemIndex: $itemIndex
    systemRateIndex: $rateIndex
  ) {
    _id
  }
}
