apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: web
  template:
    metadata:
      labels:
        component: web
    spec:
      containers:
        - name: client
          image: itsdevin/bow-mark-client:$COMMIT_SHA1
          resources:
            requests:
              cpu: "900m"
              memory: "1800Mi"
            limits:
              cpu: "1000m"
              memory: "2000Mi"
          ports:
            - containerPort: 3000
          env:
            - name: SSR_API_URL
              value: https://paving.bowmark.ca/graphql
            - name: NEXT_PUBLIC_API_URL
              value: https://paving.bowmark.ca/graphql
            - name: NEXT_PUBLIC_APP_NAME
              value: Paving
